{
  "name": "rembot",
  "version": "1.0.0",
  "description": "Discord bot that bans users who post the same message across multiple channels (raid/spam guard) and purges their recent messages.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/tracker.js && node --check src/enforce.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  },
  "license": "MIT"
}
